<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lx.boot.psy.mapper.AssessmentAssignmentMapper">

    <!-- 获取测评任务分配分页列表 -->
    <select id="getAssessmentAssignmentPage" resultType="com.lx.boot.psy.model.vo.AssessmentAssignmentVO">
        SELECT
            a.id,
            a.plan_id,
            p.name as plan_name,
            s.name as scale_name,
            v.id as version_id,
            v.version_name,
            a.user_id,
            a.group_id,
            a.assign_type,
            a.progress,
            p.start_time,
            p.end_time,
            a.status,
            a.assigned_by,
            a.create_time
        FROM
            psy_assessment_assignment a,
            psy_assessment_plan p,
            psy_scale s,
            psy_scale_version v
        <where>
            a.plan_id = p.id
            AND p.scale_id = s.id
            AND p.version_id = v.id
            <if test="queryParams.userId != null">
                AND user_id = #{queryParams.userId}
            </if>
            <if test="queryParams.groupId != null">
                AND group_id = #{queryParams.groupId}
            </if>
            <if test="queryParams.assignType != null">
                AND assign_type = #{queryParams.assignType}
            </if>
            <if test="queryParams.status != null">
                AND status = #{queryParams.status}
            </if>
        </where>
    </select>

</mapper>
